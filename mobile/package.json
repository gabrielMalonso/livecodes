{
  "name": "livecodes-android",
  "version": "1.0.0",
  "private": true,
  "description": "Personal Android build of LiveCodes",
  "license": "MIT",
  "scripts": {
    "sync": "cap sync android",
    "build": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
